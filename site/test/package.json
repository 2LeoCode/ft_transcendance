{
  "name": "app",
  "version": "1.0.0",
  "main": "/build/main.js",
  "scripts": {
    "start": "node build/main.js",
    "build": "tsc -p . --outDir build",
    "start:dev": "nodemon --watch src --exec \"npm run build && npm run start\"",
    "start:prod": "npm run build && npm run start",
    "start:development": "npm run start:dev",
    "start:production": "npm run start:prod",
    "clean": "rimraf build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.6",
    "@types/node": "^14.14.10",
    "nodemon": "^2.0.6",
    "rimraf": "^3.0.2",
    "typescript": "^4.1.3"
  }
}
